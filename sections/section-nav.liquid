{% assign sid = section.id %}
<style>
    #shopify-section-{{sid}}{
        {%if section.settings.type == 'padding' %}
            padding-top:{{section.settings.top}};
            padding-bottom:{{section.settings.bottom}};
            {%  else %} 
            margin-top:{{section.settings.top}};
            margin-bottom:{{section.settings.bottom}};
            {%  endif %} 
    } 
    #shopify-section-{{sid}} .container a{
        display: flex;
        gap: 24px;
        overflow-x: auto;
        scrollbar-width: 0;
    }
    #shopify-section-{{sid}} a{
        flex-shrink: 0;
        color: #25282A;
        font-size: 12px;
        font-weight: 400;
        line-height: 1.4; /* 140% */
        letter-spacing: 0.6px;
    }
    @media (max-width:767px){
        #shopify-section-{{sid}}{
            {%if section.settings.type == 'padding' %}
                padding-top:{{section.settings.top_m}};
                padding-bottom:{{section.settings.bottom_m}};
            {%  else %} 
                margin-top:{{section.settings.toptop_m}};
                margin-bottom:{{section.settings.bottom_m}};
            {%  endif %} 
        }   
    }
</style>

<div class="container">
    {% for block in section.blocks %}
        {% assign bs = block.settings %}
        {%  if bs.text != blank %} <a href="{{bs.link}}">{{bs.text}}</a> {% endif %}
    {% endfor %}
</div>

{% schema %}
{
    "name": "Section navigation",
    "class": "custom-section-nav",
    "settings": [
        {
            "type": "select",
            "id": "type",
            "default":"margin",
            "label": "Padding or Margin",
            "options": [
                {
                    "label": "Padding",
                    "value": "padding"
                },
                {
                    "label": "Margin",
                    "value": "margin"
                }
            ]
        },
        {
            "type": "header",
            "content": "Desktop"
        },
        {
          "type": "range",
          "id": "top",
          "label": "Top",
          "max": 200,
          "min": 0,
          "step": 2,
          "default":0
        },
        {
          "type": "range",
          "id": "bottom",
          "label": "Bottom",
          "max": 200,
          "min": 0,
          "step": 2,
          "default":0
        },
        {
            "type": "header",
            "content": "mobile"
        },
        {
          "type": "range",
          "id": "top_m",
          "label": "Top",
          "max": 100,
          "min": 0,
          "step": 1,
          "default":0
        },
        {
          "type": "range",
          "id": "bottom_m",
          "label": "Bottom",
          "max": 100,
          "min": 0,
          "step": 1,
          "default":0
        }
    ],
    "blocks": [
        {
            "type": "item",
            "name": "item",
            "settings": [
                {
                    "type": "text",
                    "id": "text",
                    "label": "Text"
                },
                {
                    "type": "url",
                    "id": "link",
                    "label": "Link"
                }
            ]
        }
    ],
    "presets": [
        {"name": "Section navigation"}
    ]
}
{% endschema %}